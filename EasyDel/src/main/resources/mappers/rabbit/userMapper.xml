<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="easydel.rabbit.mapper.user">

	<insert id="insertUser" parameterType="User">
		insert into USERS (user_id, user_name, user_password, user_gender, user_email, user_birthdate, user_phone, user_picture)
		values (#{userId}, #{userName}, #{userPassword}, #{userGender}, #{userEmail}, #{userBirthdate}, #{userPhone} #{userPicture})
	</insert>

</mapper>