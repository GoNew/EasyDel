<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="easydel.gonew.mapper.edmoneyLog">
	<insert id="insertEdmoneylog" parameterType="EdmoneyLog">
		insert into	EDMONEY_LOGS(user_id, withdraw_amount)
		values(#{userId}, #{withdrawAmount})
	</insert>
</mapper>